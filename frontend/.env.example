NEXT_PUBLIC_API_URL=http://localhost:8000
# Server Components (SSR): set on Railway to the backend HTTPS URL so the Next
# container does not fall back to localhost:8000.
# API_URL_INTERNAL=https://your-backend.up.railway.app
NEXT_PUBLIC_GOOGLE_MAPS_KEY=placeholder
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
